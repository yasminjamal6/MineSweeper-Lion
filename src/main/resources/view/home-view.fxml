<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.media.MediaView?>


<StackPane xmlns="http://javafx.com/javafx/23"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="main.controller.HomeController"
           stylesheets="@../css/home.css"
           styleClass="root-pane"
           prefWidth="1200.0" prefHeight="800.0">

    <children>
        <!-- וידאו כרקע -->
        <MediaView fx:id="videoView"
                   fitWidth="1200" fitHeight="800"
                   preserveRatio="false"
                   StackPane.alignment="CENTER" />



        <VBox alignment="CENTER"
              spacing="20.0"
              styleClass="content-wrapper">

            <!-- טופ: באנר + כותרות -->
            <VBox alignment="CENTER" spacing="16.0">

                <!-- TEAM LION – באנר דק, פחות "כפתור" -->
                <StackPane styleClass="lion-banner"
                           maxWidth="520"
                           prefWidth="520"
                           minWidth="520">
                    <VBox alignment="CENTER" spacing="4.0">
                        <HBox alignment="CENTER" spacing="8.0">
                            <Label text="🦁" styleClass="badge-emoji"/>
                            <Label text="TEAM LION" styleClass="banner-title"/>
                            <Label text="🦁" styleClass="badge-emoji"/>
                        </HBox>
                        <Label text='"Hakuna Matata - No Worries!"'
                               styleClass="banner-subtitle"/>
                    </VBox>
                </StackPane>

                <Label text="Mine Sweeper Smart" styleClass="main-title"/>
                <Label text="🌟 Legendary trivia from the savanna! 🌟"
                       styleClass="main-subtitle"/>

            </VBox>

            <Region prefHeight="90.0"/>

            <!-- כפתורי אקשן -->
            <VBox alignment="CENTER" spacing="14.0" styleClass="buttons-wrapper">
                <Button text="Start Adventure 🦁"
                        styleClass="primary-btn"
                        onAction="#onStartAdventure"/>
                <HBox alignment="CENTER" spacing="16.0">
                    <Button text="Question Manager"
                            styleClass="secondary-btn"
                            onAction="#onManageQuestions"/>
                    <Button text="Game History"
                            styleClass="secondary-btn"
                            onAction="#onHistory"/>
                </HBox>
            </VBox>
            <!-- מרווח לפני שלושת השורות -->
            <Region prefHeight="100.0"/>

            <!-- שלושת הכרטיסים -->
            <HBox alignment="CENTER" spacing="24.0" styleClass="features-row">

                <VBox styleClass="info-card" spacing="6.0" alignment="CENTER">
                    <Label text="King of the Jungle" styleClass="card-title"/>
                    <Label text="Lions rule the board!" styleClass="card-subtitle"/>
                </VBox>

                <VBox styleClass="info-card" spacing="6.0" alignment="CENTER">
                    <Label text="Pride Rock" styleClass="card-title"/>
                    <Label text="Challenge your knowledge" styleClass="card-subtitle"/>
                </VBox>

                <VBox styleClass="info-card" spacing="6.0" alignment="CENTER">
                    <Label text="Circle of Life" styleClass="card-title"/>
                    <Label text="Collect points and win" styleClass="card-subtitle"/>
                </VBox>

            </HBox>

            <!-- SPACER – דוחף את הפוטר עוד למטה -->
            <Region VBox.vgrow="ALWAYS"/>

            <!-- FOOTER -->
            <VBox alignment="CENTER" spacing="8.0" styleClass="footer-box">
                <HBox alignment="CENTER" spacing="8.0" styleClass="footer-pill">
                    <Label text="🦁" styleClass="footer-emoji"/>
                    <Label text='"Remember who you are"' styleClass="footer-quote"/>
                    <Label text="👑" styleClass="footer-emoji"/>
                </HBox>
                <Label text="Team Lion - Roaring Since 2025 🌅"
                       styleClass="footer-signature"/>
            </VBox>

        </VBox>

        <!-- Floating How To Play button (NOW ON TOP) -->
        <Button text="How to Play?"
                styleClass="how-btn"
                onAction="#onHowToPlay"
                StackPane.alignment="TOP_LEFT">
            <StackPane.margin>
                <Insets top="18" left="18"/>
            </StackPane.margin>
        </Button>
        <Button text="⚙"
                fx:id="settingsBtn"
                styleClass="settings-btn"
                onAction="#openSettings"
                StackPane.alignment="TOP_RIGHT">
            <StackPane.margin>
                <Insets top="18" right="18"/>
            </StackPane.margin>
        </Button>

    </children>

</StackPane>
